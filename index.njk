---
layout: base.njk
---
{% if site.hero and site.hero.image %}
<section class="hero" style="background-image: url('{{ site.hero.image }}');">
  <p class="hero-text">{{ site.hero.text }}</p>
  <a class="hero-cta" href="#writing">{{ site.hero.cta }} ↓</a>
</section>
{% endif %}

<section class="intro" id="writing">
  <p>{{ site.description }}</p>
</section>

<section class="post-list">
  {% for post in collections.post | reverse %}
  <article class="post-list-item">
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    <p class="post-meta">
      <time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>
      {% if post.data.category %} · <span class="post-category">{{ post.data.category }}</span>{% endif %}
    </p>
    {% if post.data.summary %}<p class="post-summary">{{ post.data.summary }}</p>{% endif %}
  </article>
  {% endfor %}
</section>
